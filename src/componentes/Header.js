import React from 'react';

export default ({tittle}) => {
  return (
            <nav>
                <div className="nav-wrapper blue">
                        <a className="brand-logo center">{tittle}</a>
                </div>
            </nav>
  )
}
